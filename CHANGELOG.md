# 升级日志（CHANGELOG）

---

## [v1.0.3] — 2026-09-15

**修复：把系统装在"子目录"里时（例如 `http://主机:端口/你的项目名/`），报表页面打不开、一片空白。**

### 升级内容

- 修复：**把报表装在子目录下时，页面一片空白、什么也点不动**。
  原因：报表页面里"去哪儿取文件、去哪儿调接口"的地址，以前都写成从网站最外层数起的固定写法 ——
  好比信上只写了门牌号、没写小区名，系统装在子目录里时，浏览器就跑到隔壁小区去找了，找不到自然是空白。
  现在这些地址改成**跟着实际部署位置走**：装在根目录、装在子目录（一层或多层）都自适应，不用改配置。
- 修复：**上传的电子印章显示成"裂图"（一个碎图标）**，设计器画布和右侧印章面板都一样。
  原因：印章图片在模板里存的是一个简短地址，装在子目录下时需要给它补上子目录名；上一版补的时候**补重复了**
  （等于把小区名写了两遍），浏览器按这个地址去找，自然找不到图片。
  现在已经改成只补一次：设计器画布、印章面板、预览、打印四处都能正常显示；
  **以前做好的报表不用重新上传印章、也不用改模板**。
- 修复：**打印时凭空多出几乎空白的纸（明明是 3 页，打出来 4 张甚至 6 张）**。两个原因：
  ① 自动分页时按"行高设置值"算高度，可文字比行高大、或者换行时，行会被顶高
  （设置了 32 像素的行，实际可能占 54 像素），算少了于是把内容挤到下一张纸，多出来的那一点
  被打印机单独吐成一张几乎空白的纸。现在改成按"实际会占多高"来算，宁可多算一点、不少算。
  ② 页内容区的高度不能按"整张纸"顶满：打印对话框默认带边距，可打印区域比整张纸小，
  顶满就一定会被"多切一刀"，每页后面各多一张空白纸（3 页打成 6 张就是这么来的）。
  现在页内容区改成"内容多高就多高、最多不超过一张纸"，并额外留出 **22 毫米安全余量**
  （比对话框默认边距的 20 毫米更多），所以「边距」选「无」还是保留默认值都不会再多吐空白页；
  内容仍旧自上而下顺排，套打位置不受影响。
- 修复：**印章勾了「跟随明细」时，表头、表尾也被多盖了一枚章**（明细只有 1 条的单据，打印出来有 3 个章）。
  原因：判断"这一行要不要盖章"时，把紧贴着明细行的表头行、表尾行也算进明细范围了。
  现在跟随明细只盖明细行本身：明细 1 行盖 1 枚、明细 12 行盖 12 枚；
  「一枚章盖整张单据」和「贴单据上边 / 下边」的用法不受影响。
- 小提醒：打印对话框里的**纸张（如 A4 横向 297×210）／边距「无」／缩放 100%／每版 1 页**最好都选对，
  套打定位尤其依赖「边距＝无」（打印内容上有一条黄色自检提示，不会被印到纸上，可以照着核对）。
- 升级注意：无，换 jar 重启即可；前端页面随 jar 一起更新，浏览器强刷一次（Ctrl+F5）即可。

### 数据库变动

**无**。

---

## [v1.0.2] — 2026-09-15

**修复：嵌进"自己接管了静态文件目录"的系统后，报表页面打不开（空白，或提示找不到页面）。**

### 升级内容

- 修复：**报表页面打不开（一片空白，或提示找不到页面）**。
  原因：有些系统会统一接管"网站上的静态文件从哪个目录取"。报表的页面文件是随 jar 包一起发布的，
  被这种接管之后，文件虽然还在包里，却没有人去找它 —— 页面自然就打不开。
  现在**报表自己声明"报表的页面由我来取"**，不再受这类接管规则影响。
- 修复：报表里的子页面（报表列表、预览页等）**刷新一下、或者把网址发给别人打开时打不开** ——
  现在统一交回入口页处理，刷新、收藏、粘贴网址进来都正常。
- 升级注意：无，换 jar 重启即可。

### 数据库变动

**无**。

---

## [v1.0.1] — 2026-09-15

**这一版的重点：程序不再自己去动你数据库里的表。** 建表与升级改成"由你执行 SQL 脚本"，程序默认什么都不做。

### 升级内容

- **新增配置 `report.metadata.auto-init`（默认 `false`）** —— 以前应用启动会自己把元数据表建出来，现在**默认不建**：
  - 关闭时（默认）启动阶段**完全不碰你的数据库**：不建表、不改表、不探测，只打一行日志告诉你去跑哪份脚本；
  - 想恢复"让程序自己把缺的表建出来"，把它设成 `true` 即可（演示程序就是这么配的）。
- **表结构与升级改成 SQL 脚本**（新增 `db/` 目录）：
  - `db/mysql/tb-report-metadata.sql` —— **最新版本的全量建表脚本**（6 张表、逐字段中文注释、可重复执行）；
  - `db/mysql/upgrade/vX.Y.Z.sql` —— **每个版本的升级脚本**（文件名 = "从上一版升到该版本"），按版本号从小到大执行。
- **`report.metadata.auto-upgrade`（默认 `false`）** 只管"改**已存在**的表"（补字段 / 放宽大字段），也只在
  `auto-init=true` 时才有意义；不打开时只把该执行的改表语句写进启动日志，由你自己决定跑不跑。
- **不再自动改表名**：以前遇到"用旧前缀建的表"可能会自动改名，现在**只提示**，并给你一句可以直接复制执行的改名语句 ——
  库里可能躺着别的系统的表，光看名字猜归属太不安全，改不改由你定。
- 修复：老库缺字段时，启动日志里会连打一串报错（其实是"给字段补中文注释"的语句没执行成功）——
  现在改成先补齐字段、再补注释，字段还不存在就直接跳过，不再刷报错。

### 数据库变动

**无** —— 表结构与 v1.0.0 完全一致，**升级到本版不需要执行任何 SQL**。

- 新装：执行 [`db/mysql/tb-report-metadata.sql`](db/mysql/tb-report-metadata.sql) 建表（或设 `report.metadata.auto-init=true` 让程序建）。
- 若是 v1.0.0 之前用过 0.0.x 快照版的老库：见 [`db/mysql/upgrade/v1.0.0.sql`](db/mysql/upgrade/v1.0.0.sql)。

---

## [v1.0.0] — 2026-09-15

**首个正式版本**：可嵌入 Spring Boot 3 的轻量低代码报表工具（**永久免费**：不限期、不收费，见 LICENSE 2.6）。

### 报表设计

- 可视化设计器：单元格样式/边框/合并、拖选与填充柄（像 Excel 一样续号/复制）、撤销重做、格式刷、数字格式、缩放；
- **循环块**（明细按数据自动展开）、**主子表**（主记录与明细联动）、分组汇总、统计（求和/计数等）；
- **二维码 / 条形码**插入到单元格，内容可用表达式；
- 同一模板里主记录与明细块可嵌套，页面上按"一份单据"分页。

### 数据取数

- 数据集四种来源：**SQL**（支持 `${参数}` 占位）、**HTTP 接口**（主子表嵌套 JSON，配 `dataPath`/`detailPath`）、**静态 JSON**、**JavaBean**；字段明细自动解析；
- **数据字典**：字段值 → 显示名（手工维护 / 接口 / SQL 三种来源），取数后统一翻译，预览与打印一致；
- 数据源在配置文件声明，连接密码支持 `ENC(密文)` / 环境变量 / 明文三种写法。

### 打印与输出

- **1:1 套打**：设计尺寸就是纸上尺寸，不缩放；页面内显示所选纸张的**可打印区参考线**；
- 打印设置：纸张（基础资料里统一维护宽高）、方向、边距、页眉页脚、每页行数、手动分页；
- **每页重复表头 / 表尾**；**每页页码 / 本单据页码**；
- **电子印章**：在设计器里定位，随单元格 / 明细行自动盖到每一份单据上；
- **打印后回调业务接口**：打印完自动通知你的系统（POST/GET，带本次页面参数与登录凭证），用于"打一张单据、自动改一次状态"；
- **C-Lodop 插件打印**：非标纸 / 套打纸 1:1 打印（浏览器纸张选不到时用）；
- **Excel 导入 / 导出**：在 Excel 里把表格排好版可直接导入成报表模板；导出带样式与合并的 xlsx。

### 集成与部署

- 引入一个依赖 `tb-report-spring-boot-starter` 即可嵌进 Spring Boot 3 项目，**前端页面随 jar 发布**，不用单独部署前端；
- 元数据 6 张表（`tb_report` / `tb_report_dataset` / `tb_report_datasource` / `tb_report_paper` / `tb_report_dict` / `tb_report_dict_item`），启动自动建表（**v1.0.1 起默认改为由你执行脚本建表**）；也提供可直接执行的 MySQL 建表脚本 `db/mysql/tb-report-metadata.sql`；
- 表名前缀可配（`report.table-prefix`，默认 `tb_report_`），避免与你库里已有的表重名冲突；
- 可挂登录态：实现一个 `ReportAccessProvider` Bean 即可接入你自己的鉴权。

### 数据库变动

- **新装**：执行全量建表脚本 [`db/mysql/tb-report-metadata.sql`](db/mysql/tb-report-metadata.sql)（MySQL 8 方言、6 张 `tb_report*` 表、逐字段中文注释、可重复执行）。
- **从旧版本升级**：无（首版）。v1.0.0 正式发布前用过 0.0.x 快照版的老库，另见 [`db/mysql/upgrade/v1.0.0.sql`](db/mysql/upgrade/v1.0.0.sql)。

### 许可

- **永久免费**（协议 2.6「免费使用且不限期」；6.3 明确"许可方的责任上限＝你实际支付的费用"）；LICENSE 属于**源码可见许可**，不是 OSI 开源协议。
