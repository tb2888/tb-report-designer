# 升级日志（CHANGELOG）

> 一个文档累加，**新版本写在最上面（倒序）**。
> 每一版必须写两段：**① 升级内容**（多了什么 / 修了什么 / 升级要注意什么）+ **② 数据库变动**（有库表变动就给出**可直接执行的升级 SQL**，没有就写「无」）。
> 发版前先写好条目再跑 `.\release.ps1 -Version x.y.z` —— 脚本会校验该版本条目存在、且有「### 数据库变动」小节，缺了直接中止发版。

## 每一版按这个格式写（复制下面这段改）

````markdown
## [v1.1.0] — 2026-10-01

### 升级内容
- 新增：……
- 修复：……
- 升级注意：……（老用户需要手工做什么）

### 数据库变动
无

<!-- 有库表变动时写成这样（必须是能直接执行、可重复执行的 SQL）：
新增「字段备注」列：

```sql
ALTER TABLE tb_report_dataset ADD COLUMN field_remark VARCHAR(512) NULL COMMENT '字段备注';
```
-->
````

## 版本号怎么定

`主版本.功能版本.修订号`，例如 `1.0.0` → `1.1.0` → `1.1.1`：

| 位置 | 什么时候 +1 | 例子 |
|---|---|---|
| 主版本 | 有**不兼容**改动（老模板/老数据要手工处理） | 报表模板结构变了 |
| 功能版本 | **新增功能** | 加了分组汇总 |
| 修订号 | 只**修 bug**、不改功能 | 修了分页少一行 |

## 分支与 tag 策略

- **`main`**：唯一的长期分支，开发主线，永远是最新开发版（GitHub 默认分支就是它）。
- **每个版本一个 tag**：`v1.0.0`、`v1.1.0`… 发版时由 `release.ps1` 自动打，钉在"发布那一刻"的提交上（**不可变**，用来定位用户下载到的那份代码；GitHub / Gitee 的 **Releases 就是挂在这些 tag 上**）。
- **不建版本分支**（和大多数项目一致）：老版本真要修 bug 时，再从 tag 临时拉一个维护分支用，例如
  `git branch 1.x v1.0.0`，修完发 `v1.0.1` tag，维护分支留或删都行。
- 确实需要给某版本一条常驻分支时，用 `.\release.ps1 -Version x.y.z -WithBranch` 建。

## 发版动作（顺序）

1. 在**下面版本列表的最上面**按格式写好新版本条目（用户能看懂的"升级内容"）；
2. `.\publish-open.ps1`：同步免费版 + 导出开源仓（会先跑一次免费版构建）；
3. `.\release.ps1 -Version 1.0.0 -Proxy http://127.0.0.1:7897`：给主仓和开源仓**打 tag + 推送**；
4. GitHub / Gitee 上建 Release：粘贴本文件里对应版本的条目，附件传 `dist\tb-report-demo-free.jar`。

---

## [v1.0.0] — 2026-09-15

**首个正式版本**：可嵌入 Spring Boot 3 的轻量低代码报表工具（**永久免费**：不限期、不收费，见 LICENSE 2.6）。

### 报表设计

- 可视化设计器：单元格样式/边框/合并、拖选与填充柄（像 Excel 一样续号/复制）、撤销重做、格式刷、数字格式、缩放；
- **循环块**（明细按数据自动展开）、**主子表**（主记录与明细联动）、分组汇总、统计（求和/计数等）；
- **二维码 / 条形码**插入到单元格，内容可用表达式；
- 同一模板里主记录与明细块可嵌套，页面上按"一份单据"分页。

### 数据取数

- 数据集四种来源：**SQL**（支持 `${参数}` 占位）、**HTTP 接口**（主子表嵌套 JSON，配 `dataPath`/`detailPath`）、**静态 JSON**、**JavaBean**；字段明细自动解析；
- **数据字典**：字段值 → 显示名（手工维护 / 接口 / SQL 三种来源），取数后统一翻译，预览与打印一致；
- 数据源在配置文件声明，连接密码支持 `ENC(密文)` / 环境变量 / 明文三种写法。

### 打印与输出

- **1:1 套打**：设计尺寸就是纸上尺寸，不缩放；页面内显示所选纸张的**可打印区参考线**；
- 打印设置：纸张（基础资料里统一维护宽高）、方向、边距、页眉页脚、每页行数、手动分页；
- **每页重复表头 / 表尾**；**每页页码 / 本单据页码**；
- **电子印章**：在设计器里定位，随单元格 / 明细行自动盖到每一份单据上；
- **打印后回调业务接口**：打印完自动通知你的系统（POST/GET，带本次页面参数与登录凭证），用于"打一张单据、自动改一次状态"；
- **C-Lodop 插件打印**：非标纸 / 套打纸 1:1 打印（浏览器纸张选不到时用）；
- **Excel 导入 / 导出**：在 Excel 里把表格排好版可直接导入成报表模板；导出带样式与合并的 xlsx。

### 集成与部署

- 引入一个依赖 `tb-report-spring-boot-starter` 即可嵌进 Spring Boot 3 项目，**前端页面随 jar 发布**，不用单独部署前端；
- 元数据 6 张表（`tb_report` / `tb_report_dataset` / `tb_report_datasource` / `tb_report_paper` / `tb_report_dict` / `tb_report_dict_item`），启动自动建表；也提供可直接执行的 MySQL 建表脚本 `db/mysql/tb-report-metadata.sql`；
- 表名前缀可配（`report.table-prefix`，默认 `tb_report_`），避免与宿主表冲突；
- 可挂登录态：实现一个 `ReportAccessProvider` Bean 即可接入你自己的鉴权。

### 数据库变动

- **首次安装**：元数据 6 张表由应用启动时自动创建；也可手工执行 `db/mysql/tb-report-metadata.sql`（MySQL 8 方言、含表与字段注释、可重复执行）。
- **从旧版本升级**：无（首版）。

### 许可

- **永久免费**（协议 2.6「免费使用且不限期」；6.3 明确"许可方的责任上限＝你实际支付的费用"）；LICENSE 属于**源码可见许可**，不是 OSI 开源协议。
